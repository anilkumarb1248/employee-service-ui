import { AccessTypesPipe } from './access-types.pipe';

describe('AccessTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new AccessTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
